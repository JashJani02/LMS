# LMS

## Roles
1) Admin (controles the whole site)
2) Instructore (CRUD, grading operations on Courses and Quizes)
3) Student (Attend lecture videos and quizes)

## Tech-Stack
1) React, HTML, Tailwind (Frontend)
2) FastAPI OR Node (Backend)
3) SQL (Database)
4) JWT OR Firebase (Auth)

``` mermaid

flowchart TD
	A(["Start: Build LMS (MVP)"]) --> B["Understand requirements from SRS<br>Roles: Admin / Instructor / Student<br>Modules: Auth, Courses, Enrollment, Lectures, Assignments, Quizzes, Certificates"]
	B --> C["Define scope constraints (MVP)"]
	C --> C1[FREE courses only]
	C --> C2[No payments]
	C --> C3[No chat/messaging]
	C --> C4[No notifications]
	C --> C5["No advanced analytics (Phase 2)"]
	B --> D["Finalize tech stack + deployment"]
	D --> D1["Frontend: React + TypeScript"]
	D --> D2[Backend: FastAPI]
	D --> D3["DB: SQLite (VPS file)"]
	D --> D4["Auth: Custom JWT (access + refresh)"]
	D --> D5["Frontend deploy: Cloudflare Pages"]
	D --> D6["Backend deploy: VPS (Ubuntu + Nginx + Gunicorn/Uvicorn + HTTPS)"]
	D --> D7["Uploads storage: VPS /uploads"]
	B --> E["Phase 1: System design"]
	E --> E1[High-level architecture]
	E --> E2["Database design (UUID IDs, core tables)"]
	E --> E3["Backend architecture rules<br>Routers=no business logic<br>Services=business rules<br>Repositories=DB access<br>Dependencies=RBAC guards"]
	E --> E4[Frontend routes by role]
	B --> F["Phase 2: Setup project foundations"]
	F --> F1["Create FastAPI project structure (modules, schemas, services, repos)"]
	F --> F2["Setup DB session + models (SQLite)"]
	F --> F3["Setup file handling for /uploads (submissions, certificates)"]
	F --> F4["Create React app + routing skeleton"]
	B --> G["Phase 3: Implement MVP modules (recommended order)"]
	G --> H[3.1 Authentication Module]
	H --> H1["Register/Login"]
	H --> H2[Password hashing]
	H --> H3[JWT access/refresh]
	H --> H4["Role-based access control (Admin/Instructor/Student)"]
	G --> I[3.2 User Roles & Permissions]
	I --> I1["Admin: observe everything, suspend users"]
	I --> I2["Instructor: manage courses + grading + certificates"]
	I --> I3["Student: enroll + learn + submit + attempt quizzes + request certificate"]
	G --> J[3.3 Course Module]
	J --> J1[Create course]
	J --> J2["Publish/Unpublish"]
	J --> J3[Weekly sections]
	J --> J4[Ordered lectures]
	J --> J5["Store video as URL reference (not video files in DB)"]
	G --> K["3.4 Enrollment + Progress Tracking"]
	K --> K1[Enroll student]
	K --> K2["Track progress % (lectures completed)"]
	K --> K3[Enrollment history]
	G --> L[3.5 Assignment Module]
	L --> L1["Instructor creates assignment<br>due date + max marks"]
	L --> L2[Student submits multiple attempts]
	L --> L3["No overwrite; keep submission history"]
	L --> L4["Instructor grades + feedback"]
	G --> M["3.6 Quiz Module (MCQ)"]
	M --> M1["Instructor creates quiz<br>#questions, due date/time, optional time limit"]
	M --> M2[Student attempts multiple times before due date]
	M --> M3[Block attempts after due date]
	M --> M4[Store each attempt permanently]
	M --> M5["Snapshot answers/questions per attempt<br>prevents edits corrupting history"]
	G --> N[3.7 Certificate Module]
	N --> N1["Eligibility checks<br>lectures completed + required submissions + quiz attempted (if any)"]
	N --> N2["Student requests certificate -> status PENDING"]
	N --> N3["Instructor reviews -> APPROVE/REJECT"]
	N --> N4["On approval: generate PDF"]
	N --> N5["Store to /uploads/certificates"]
	N --> N6["Generate verification code + endpoint /verify/{certificateCode}"]
	B --> O["Phase 4: Frontend implementation"]
	O --> O1["Public pages: /login /register /courses /course/:id"]
	O --> O2["Student: /dashboard /my-courses /course/:id/learn /quiz /assignment /certificate"]
	O --> O3["Instructor: /instructor/dashboard /course/create /course/:id/manage /students /assignments /quizzes /certificates"]
	O --> O4["Admin: /admin/dashboard /users /courses"]
	O --> O5["API integration (Axios/React Query)"]
	B --> P["Phase 5: Non-functional requirements hardening"]
	P --> P1[Input validation on all endpoints]
	P --> P2[Prevent submissions after due date]
	P --> P3[Prevent quiz attempts after due date]
	P --> P4["Prevent instructor editing others' courses"]
	P --> P5[Secure file upload validation]
	P --> P6["Indexed foreign keys (where applicable) + integrity checks"]
	P --> P7[UUID everywhere]
	B --> Q["Phase 6: Testing"]
	Q --> Q1["Unit test services (business rules)"]
	Q --> Q2["API tests for RBAC + due date rules"]
	Q --> Q3["Happy path E2E: enroll -> learn -> submit -> quiz -> request certificate -> approve -> download"]
	B --> R["Phase 7: Deployment"]
	R --> R1[Deploy backend to VPS]
	R --> R2[Setup Nginx reverse proxy]
	R --> R3[HTTPS via Certbot]
	R --> R4["Ensure SQLite file + /uploads persist on VPS"]
	R --> R5[Deploy frontend to Cloudflare Pages]
	B --> S["Phase 8: Post-MVP / Future phase"]
	S --> S1[Analytics dashboard]
	S --> S2[Email notifications]
	S --> S3[Instructor analytics]
	S --> S4[Course reviews]
	S --> S5[Migrate to PostgreSQL]
	S --> S6["Move uploads to object storage (Cloudflare R2)"]
	S --> T(["End: LMS MVP live"])

```